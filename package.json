{
  "name": "art-and-craft",
  "version": "1.0.0",
  "description": "Art & Craft E-commerce",
  "scripts": {
    "dev": "node ./node_modules/concurrently/dist/bin/index.js \"npm run dev --prefix storefront\" \"npm run dev --prefix admin\"",
    "dev:storefront": "npm run dev --prefix storefront",
    "dev:admin": "npm run dev --prefix admin"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "serve": "^14.2.1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.2"
  }
}
